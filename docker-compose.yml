version: "3.4"
services:

  git_stats:
    image: gitlab_stats
    container_name: gitstats
    env_file: secrets.env
    environment:
      - GRAFANA_FREQ=900
      - GIT_URL=https://git.statoil.no
    restart: unless-stopped
